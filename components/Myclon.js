import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Myclon = () => {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
            <Text>Myclon page!</Text>
        </View>
    )
}

export default Myclon

const styles = StyleSheet.create({})
